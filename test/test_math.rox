function main() -> none {
    print(numToString(num32_abs(-5n32))); // 5
    print(numToString(num32_min(10n32, 5n32))); // 5
    print(numToString(num32_max(10n32, 5n32))); // 10

    // num32_pow
    rox_result[num32] p32 = num32_pow(2n32, 3n32);
    if (isOk(p32)) {
        print(numToString(getValue(p32))); // 8
    } else {
        print(any_to_string("Error in num32_pow"));
    }

    print(numToString(num_abs(-5))); // 5
    print(numToString(num_min(10, 5))); // 5
    print(numToString(num_max(10, 5))); // 10

    // num_pow
    rox_result[num] p64 = num_pow(2, 3);
    if (isOk(p64)) {
        print(numToString(getValue(p64))); // 8
    }

    print(any_to_string(float_abs(-5.5))); // 5.5
    // float_pow
    print(any_to_string(float_pow(2.0, 3.0))); // 8.0

    // float_sqrt
    rox_result[float] s = float_sqrt(4.0);
    if (isOk(s)) {
        print(any_to_string(getValue(s))); // 2.0
    } else {
        print(any_to_string("Error in sqrt"));
    }

    print(any_to_string(float_floor(2.9))); // 2.0
    print(any_to_string(float_ceil(2.1))); // 3.0
    print(any_to_string(float_exp(1.0))); // ~2.718...

    // float_log
    rox_result[float] l = float_log(e);
    if (isOk(l)) {
        print(any_to_string(getValue(l))); // 1.0
    }

    print(any_to_string(float_sin(0.0))); // 0.0
    print(any_to_string(float_cos(0.0))); // 1.0
}
